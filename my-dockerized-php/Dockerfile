# Use the official PHP image with Apache
FROM php:8.0-apache

# Copy the PHP script to the web server directory
COPY index.php /var/www/html/

# Use the default Apache configuration
# The server will start automatically when the container is run
